{{ knp_menu_render('BlogBundle:Builder:mainMenu') }}

{% block body %}

    {% for post in posts %}
        <h2>{{ post.title }} </h2>
        <h5>at {{ post.createdAt | lower }}</h5>
        <p>{{ post.content }} by {{ post.author | upper }}</p>
    {% endfor %}

{% endblock %}